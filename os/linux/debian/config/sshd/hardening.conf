# SSH hardening — managed by dotfiles
# deployed to /etc/ssh/sshd_config.d/99-hardening.conf

# disable root login entirely
PermitRootLogin no

# key-based auth only — no passwords
PasswordAuthentication no

# listen on IPv4 only
AddressFamily inet
