#!/usr/bin/env bash

set -euo pipefail

if [[ $# -ne 1 ]]; then
  echo "usage: urlcheck URL" 1>&2
  exit 1
fi

curl -sS -L -o /dev/null -w 'status: %{http_code}\nredirects: %{num_redirects}\ntime: %{time_total}s\n' "$1"
