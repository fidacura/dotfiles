#!/usr/bin/env bash

set -euo pipefail

if [[ $# -lt 1 ]]; then
  echo "usage: countdown SECONDS [MESSAGE]" 1>&2
  exit 1
fi

remaining=$1
message=${2:-done}

while [[ $remaining -gt 0 ]]; do
   echo -ne "$remaining\033[0K\r"
   sleep 1
   : $((remaining--))
done

echo -e "\033[0K$message\a"
